# Default pnpm isolated linker — do NOT change to node-linker=hoisted.
# hoisted causes duplicate React instances (useContext null error at build time).
# next build with output: "standalone" will hit EPERM on Windows without Developer
# Mode — use dev-create.sh --skip-frontend on Windows and let GitHub Actions
# (Linux) handle the standalone build.
