# Default pnpm isolated linker — do NOT change to node-linker=hoisted.
# hoisted causes duplicate React instances (useContext null error at build time).
# The standalone build EPERM on Windows is handled by making output: 'standalone'
# conditional on NEXT_OUTPUT=standalone (set only in Linux/CI deploy scripts).
